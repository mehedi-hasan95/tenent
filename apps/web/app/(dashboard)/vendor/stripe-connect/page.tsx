import { StripeConnectSection } from "@/components/stripe/stripe-connect-section"

const Page = async () => {
  return (
    <div>
      <StripeConnectSection />
    </div>
  )
}

export default Page
